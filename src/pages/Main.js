import React from "react";
import Calendar from "../components/Calendar";

const Main = () => {
    return (
        <React.Fragment>
            <Calendar />
        </React.Fragment>
    );
};

export default Main;
